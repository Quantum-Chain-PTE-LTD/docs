The Quantum Chain Custody API uses Dilithium3 (ML-DSA-65) post-quantum signatures for all wallet operations. Keys are never held by the API — your application signs transactions externally and submits signatures via the `/transactions/{id}/signature` endpoint.
