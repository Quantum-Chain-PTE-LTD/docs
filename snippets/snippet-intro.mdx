The Quantum Chain Custody API uses post-quantum signatures for all wallet operations. Keys are never held by the API. Your application signs transactions externally and submits signatures via the `/transactions/{id}/signature` endpoint.
